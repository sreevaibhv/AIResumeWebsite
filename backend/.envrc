source_up

# The root .envrc exports ANTHROPIC_API_KEY for Claude Code. dotenv does not
# override vars already present in process.env, so leaving it set here would
# make the backend use Claude Code's key instead of the one in backend/.env.
unset ANTHROPIC_API_KEY
